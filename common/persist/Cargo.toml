[package]
name = "evm-persist"
description = "Which state-change records in a Firehose Extended EVM block survived execution"
edition = { workspace = true }
version = "0.1.0"
license = { workspace = true }

[lib]
crate-type = ["rlib"]

[dependencies]
substreams = { workspace = true }
substreams-ethereum = { workspace = true }

[dev-dependencies]
hex = "0.4"
prost = { workspace = true }
