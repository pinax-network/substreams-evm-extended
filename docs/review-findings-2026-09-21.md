# Hardening review, 2026-09-21 (partial, unverified)

Status: **partial**. A review workflow (20 finders: five crates x four dimensions, each finding then judged by three
adversarial verifiers; plus six solc storage-layout extractors) was launched against the crates merged in PRs #33-#38.
The account spend limit stopped it after **4 of 110 agents** had finished: the four finders for
`compound-v3/balance-state` returned 28 findings; **none of the verifier agents ran**, and no finder ran for
`compound-v2/balance-state`, `lido/balance-state`, `erc4626/balance-state` or `common/retention`, and no layout
extractor ran. The findings below are therefore reviewer claims, not confirmed defects, except where the note says
that the maintainer re-derived them.

The same shape of defect must be assumed possible in the sibling crates until they are reviewed: the finders were told
to look at exactly the properties listed under "Dimensions" in [`handoff.md`](handoff.md#hardening-review-status).

The one finding reported independently by all four reviewers is treated as **confirmed by convergence** and is the
first item of the fix list in `handoff.md`: the committed cUSDCv3 epoch reviews no scalar slots, but the pinned
implementation writes `keccak256("comet.reentrancy.guard")` (`0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac`,
`CometCore.sol:60`) on every `supply`/`withdraw`/`transfer`/`buyCollateral`, so any real block with Comet activity
would fail closed as `unresolved storage`. The synthetic tests cannot see it because none writes a scalar reviewed slot.

Update 2026-09-21 (later the same day): every `compound-v3` finding below was addressed in the
"compound-v3 hardening" PR (guard slot reviewed by name, one row per decoded field, holder rows for
every written word, INVALIDATED rows instead of failing the block, constant cross-checks, producer
versions 4/5 only, contextual reduce() messages, the int104-min negation refusal in `conformance::comet`,
and the listed tests). The "balance-state consistency" PR then applied the sibling notes: unconditional decoded-field rows in `lido` and `erc4626`, producer versions 4/5 in every map crate, INVALIDATED rows in `aave/balance-state`, `blocks_per_year` pinned and `global_carryover = false` in `compound-v2`, contextual `reduce()` messages everywhere, and a shared hardening test per crate.

### Hand review of the sibling crates (2026-09-22)

The automated review of `compound-v2`, `lido`, `erc4626` and `common/retention`
was launched twice and lost every agent to the account spend limit, so the
maintainer reviewed the one dimension that had already produced a real defect
in `compound-v3`: **which slots do routine operations write that the fixture
does not cover?**

Found and fixed (`erc4626`, high): the OpenZeppelin vault fixture covered only
three of the five `openzeppelin.storage.ERC20` members and none of
`openzeppelin.storage.ERC4626`. `__ERC20_init_unchained` writes `_name` (+3)
and `_symbol` (+4) and `__ERC4626_init_unchained` writes the `ERC4626Storage`
word (`_asset`, `_underlyingDecimals`), so any block that initializes or
re-initializes an upgradeable vault was refused as `unresolved storage`. The
metadata slots are now reviewed, the `ERC4626Storage` word is bound as an
invalidating pointer (rebinding `_asset` changes what the vault converts into,
so it must not be silently accepted either), and a layout test now asserts that
every namespace member is decoded, reviewed or guarded.

Checked and found correct: `compound-v2` and `lido` cannot have the same class
of defect, because their `tests/storage_layout.rs` completeness assertions
already require every compiled slot (cToken 0–18, JumpRateModelV2 0–4) and
every Lido `*_POSITION` constant (16 of them) to be decoded or reviewed, and
neither contract family uses storage outside those sets. **Corrected by the
independent reviews below:** Lido's `finalizeUpgrade_v4` wipes two
function-local v3 positions no layout lists, and the deployed cUSDC/cETH do
not run the pinned compound-v2 source at all.

The other three dimensions (pinned-source conformance, contract conformance,
test adequacy) of those four crates remain unreviewed by an independent reader.

### Independent review of `common/retention` (2026-09-22)

One fresh-context reviewer read `common/retention` against
`initialization-and-completeness.md`, the balance-state contract and the #7
acceptance list, and proved each claim with a probe test in a scratch copy of
the crate. It reported 12 findings and no atomicity or undo-restoration
defect. The maintainer re-read each code path; all 12 were confirmed and
fixed, and one more was found while fixing them (the last row).

| # | Severity | Finding | Fix |
| --- | --- | --- | --- |
| 1 | medium | A deployment-zero seed was refused whenever the creation block emitted a row for the token (a constructor mint), so the origin was unusable for most tokens | Holders the creation block wrote keep their row; the others are seeded. Test `a_constructor_mint_keeps_its_row_and_the_other_holders_start_at_zero` |
| 2 | medium | A `BOUND` without carryover dropped a pre-existing market's entries, after which it could be seeded as newly deployed | The ledger keeps the first block at which it held any state per contract (journaled). Test `a_token_with_earlier_state_is_never_new_again_even_after_its_entries_are_dropped` |
| 3 | medium | `evm.balances.v1` amounts and `evm.balance_state.v1` bases shared one key space, suspension map and unsupported set | `Ledger::new(undo_depth, Domain)`: one ledger per output; checkpoint units follow the domain |
| 4 | medium | Epoch numbers were ignored: an older epoch's `INVALIDATED` suspended a newer binding, an older `BOUND` resumed it, and an older epoch's rows survived a `BOUND` without carryover | Epochs must strictly increase; rows name the epoch in force or one a `BOUND` of the same block supersedes; older-epoch rows apply just before that `BOUND` |
| 5 | medium | `BlockClock` counts and stream identity were not checked, so a partially stored block or a switched package was accepted | Counts must equal rows; chain, package, version, spec revision and parameters hash are bound (journaled, released by undoing the first block) |
| 6 | medium | `compare(&[])` reported `bounded_parity` | New status `no_reference` |
| 7 | medium | Only the last enumeration was reported; its block was unchecked and survived undo | All enumerations reported; each must describe the latest applied block; undo discards later ones |
| 8 | medium | Entries of unsupported contracts were counted as initialized, and seeds for them were accepted | Seeds refused; entries counted separately as `unsupported_entries` |
| 9 | medium | Documented rules (epoch ordering, `SUSPENDED`, `REAFFIRMED` not resuming, `Applied` counts, `first_block`) had no tests; mutants passed; §3 over-claimed evaluation | Tests added for each; §3 now says the ledger keeps basis only |
| 10 | low/medium | `GlobalState` rows and holder enums were not validated | Known enum values, `END_OF_BLOCK` holder rows, sign only for a signed principal, decimal globals, unsupported markets refused |
| 11 | low | Checkpoints accepted negative values for unsigned balances and values beyond 256 bits | Exact uint256 / int256 decimals; sign per domain |
| 12 | low | Undoing the first block left `first_block` set | Reset when no block remains applied |
| + | medium | Rows of two epochs for one holder in an activation block were refused as duplicates | Duplicate key is `(market, epoch, holder)` |

### Independent review of `erc4626/balance-state` (2026-09-22)

A fresh-context reviewer compared the crate with the pinned StaticATokenLM,
RayMathExplicitRounding, SavingsDai, Pot and OpenZeppelin sources, the proto
and #24, and reproduced each finding with a throwaway test. The maintainer
re-derived each from the pinned source (`StaticATokenLM.sol:52,59,75,81,683`,
OpenZeppelin upgradeable `625fb3c2` `Initializable.sol:77`) or the proto; all
ten were confirmed and fixed. The earlier hand review's fix for initializing
blocks was incomplete (item 2).

| # | Severity | Finding | Fix |
| --- | --- | --- | --- |
| 1 | high | `_rewardTokens.push` (the permissionless `refreshRewardTokens()`) and long `name`/`symbol` write `keccak256(slot) + i`, which no rule reviewed: the first reward registration halted the stream. The layout test compared root slots only | `other_dynamic_slots`: an element is reviewed when the block carries the verified preimage of `keccak256(slot)` and the offset is below 2³²; the layout test now requires every `bytes`/dynamic-array slot to be listed |
| 2 | high | OpenZeppelin v5 `initializer`/`reinitializer` write the `openzeppelin.storage.Initializable` word, which the fixture did not cover | Reviewed (`0xf0c5…6a00`, re-derived by ERC-7201 in the layout test); the initializing-block test now writes it and a long name |
| 3 | medium | The Aave Pool and OZ asset implementation pointers were optional, so omitting them failed open | Pool pointer required; an OZ asset needs its pointer or an explicit `asset_not_proxy`, never for the FiatToken model |
| 4 | medium | The static aToken's `_aToken`/`_aTokenUnderlying` (select the reserve `rate()` reads) were reviewed storage; the OZ `_asset` edge was DECLARED although its word invalidates | Both are `STORAGE_POINTER` edges and invalidating pointers; the OZ edge carries the packed `(_underlyingDecimals, _asset)` word |
| 5 | medium | `ModelEpoch` named the vault as `balance_asset` with vault decimals and `basis_scale "1"`, contrary to the proto; `implementation_revision` empty | Underlying asset and its decimals; RAY for stata/sDAI, `""` for the OZ ratio; revision `2` for the static aToken |
| 6 | medium | No test fed emitted rows into `conformance::erc4626`; two-level allowance keys and a reverted child frame untested | `tests/conformance_rows.rs` evaluates all three models from emitted rows; unit tests for both cases |
| 7 | low | `ray_mul_round_up` / `ray_div_round_up` checked only the final sum, so `(a·b) + RAY` overflow went unnoticed | Checked in Solidity's order; boundary test |
| 8 | low | The OZ `source_pin` cited the non-upgradeable `ERC4626.sol` | Pins openzeppelin-contracts-upgradeable `625fb3c2` |
| 9 | low | The decimals-offset declaration had ordinal 0 on an activation-ordinal BOUND and named the vault instead of the implementation | Uses the activation ordinal and the implementation when bound |
| 10 | low | The contract's sDAI example put Pot `chi`/`rho` at slots 2/3 | Slots 4/7 and the RAY basis scale |

### Independent review of `lido/balance-state` (2026-09-22)

A fresh-context reviewer checked the crate against lidofinance/core
`2da0f48f`, aragonOS `f3ae59b0`, the proto and #23, with a scratch test per
finding. It found no defect producing a wrong balance or refusing a routine
block (a synthetic oracle report, `submit`, `permit` and external mint pass).
All seven findings were confirmed and fixed.

| # | Severity | Finding | Fix |
| --- | --- | --- | --- |
| 1 | medium | The `DERIVED` total pooled ether is emitted only when all three words are written, but the contract's "latest row per key" rule would carry a stale one forward | Proto and contract: a `DERIVED` row is valid at its own block only and is re-derived from the latest inputs |
| 2 | medium | `conformance::lido` accepted `2^128 - 1`; the getters require `< UINT128_MAX` (`StETH.sol:318,330`) | Strict bound; the test that pinned the wrong result is flipped |
| 3 | medium | A heartbeat restates an epoch the map already invalidated (the map is stateless) | Proto and contract: `REAFFIRMED` restates configuration, never attests validity or resumes; a joining consumer reads the stored rows since activation (the retention ledger already never resumes on `REAFFIRMED`) |
| 4 | medium | `finalizeUpgrade_v4` writes the version 3 → 4 and wipes two function-local v3 positions (`Lido.sol:311-341`) that no layout lists: an early activation ordinal halted the stream, and the 3 → 4 write was a plain row | Any version write inside the epoch is `CONTRACT_VERSION_SET`; a retired-position write is `STORAGE_MIGRATION`; README: the ordinal belongs after the migration |
| 5 | medium | Kernel slots never pinned to literals; the reviewed-scalar path tested only with a slot Lido never writes; no routine-shaped block; no `u128::MAX` halves; topic count untested; no burn or zero-share conformance case | Tests for each |
| 6 | low | `Dependency` rows omitted `depth` from the proto sort key | Sorted by `(market, epoch, depth, role, contract)` |
| 7 | low | The version declaration named the proxy and a slot with ordinal 0; a succeeded transaction without a receipt dropped report evidence silently; README overstated the Accounting check | Implementation, no slot, activation ordinal; missing receipt with stETH logs fails the block; README corrected |

### Independent review of `compound-v2/balance-state` (2026-09-22)

A fresh-context reviewer compared the crate with compound-protocol
`a3214f67` and its `networks/mainnet-abi.json`, the proto and #14, with a
probe crate and conformance mutants. Nine of ten findings were confirmed as
stated and fixed; one mutant is equivalent (row 6).

| # | Severity | Finding | Fix |
| --- | --- | --- | --- |
| 1 | **high** | The deployed cUSDC and cETH do not run the pinned `^0.8.10` tree: their ABI (`decimals` uint256, public `initialExchangeRateMantissa`, three-field `AccrueInterest`) is the 2019 source, whose `ReentrancyGuard._guardCounter` takes slot 0 and shifts every word from `admin` up by one. With the fixture's slots a mint failed the block, transfers were silently read as allowances, accrual words were mislabelled and a rate-model change surfaced as an exchange-rate value. The reviewer's layout was recalled; the maintainer compiled the 2019 tree (`f385d719`, solc 0.5.17) and it matches exactly | Fixture re-pinned to the compiled 2019 layout, committed as evidence and pinned by the layout test; the 0.8.10 layout stays for current-tree delegators |
| 2 | medium | cETH's rate model is the 2019 per-year WhitePaper model (not in the pin, not in conformance); cUSDC's is `LegacyJumpRateModelV2`, a file absent from the pin | `LegacyJumpRateModelV2` found at `4caf72a1` and compiled (same storage and arithmetic as `BaseJumpRateModelV2`); new `IRM_BASE_RATE_PER_YEAR` / `IRM_MULTIPLIER_PER_YEAR` fields; `conformance::compound_v2::{WhitePaper2019, RateModel, CTokenRevision}` with the 2019 cap 5e14 |
| 3 | medium | After an invalidation the epoch kept decoding the block, so an upgrade's `_becomeImplementation` failed the block and lost the evidence; the heartbeat reaffirms an invalidated epoch; one epoch per market per parameter set | The first invalidation ends the epoch at its ordinal for the rest of the block. `REAFFIRMED` semantics were documented in #51. Multiple epochs per market remain a cross-package design item (handoff §6) |
| 4 | medium | The USDC implementation pointer was enforced but never declared | `underlying.implementation` parameter and a depth-2 `IMPLEMENTATION` pointer edge; the cToken's `underlying` word is now a pointer too |
| 5 | medium | `ModelEpoch` named the cToken as `balance_asset` with cToken decimals and scale `"1"` | Underlying (empty for native ether) with its decimals; `basis_scale` 1e18 |
| 6 | medium | Four conformance mutants survived | Exact vectors computed outside the crate kill the guard, ceil and block-delta mutants. The `<=` → `<` kink mutant is equivalent: both branches yield `kink × multiplier / 1e18 + base` at `util == kink` |
| 7 | low/medium | Missing map tests (accrual-only block, system/block scopes, delegate code change, native cash under activation ordinal, missing preimage); a dead carry-edge fallback | Tests added; the fallback replaced with a carrying add |
| 8 | low | Native `TOTAL_CASH` rows named storage slot 0 (cETH's guard word) | No slot |
| 9 | low | Declarations had ordinal 0 at a mid-block BOUND | Activation ordinal |
| 10 | low | Parameter decimals accepted leading zeros and more than 78 digits; README called WhitePaper parameters immutables | Canonical uint256 decimals; README corrected |

Legend: severity is the reviewer's; "applies to siblings" is the maintainer's note on where the same pattern exists.


## Finder `review:compound-v3:source`

5 findings, 18 properties checked and found correct.

### Findings

- **[high]** `compound-v3/balance-state/tests/fixtures/mainnet-cusdcv3-epoch.json:20` (fail-closed-on-routine-block)  
  The pinned implementation writes a raw reentrancy-guard slot keccak256("comet.reentrancy.guard") on every supply/withdraw/transfer/buyCollateral, and the fixture lists no `other_slots`, so every such transaction makes the map refuse the block as 'unresolved storage'.
  - Evidence: Pinned CometCore.sol:60 `bytes32 internal constant REENTRANCY_GUARD_FLAG_SLOT = bytes32(keccak256("comet.reentrancy.guard"));` (:63-64 NOT_ENTERED = 0, ENTERED = 1). Pinned CometWithExtendedAssetList.sol:170-201: `modifier nonReentrant() { nonReentrantBefore(); _; nonReentrantAfter(); }`, `nonReentrantBefore` does `assembly { sstore(slot, REENTRANCY_GUARD_ENTERED) }`, `nonReentrantAfter` does `assembly { sstore(slot, REENTRANCY_GUARD_NOT_ENTERED) }`. The modifier is on `supplyInternal` (:737), `transferInternal` (:848), `withdrawInternal` (:959) and `buyCollateral` (:1122), i.e. every supply*, withdraw*, transfer*, transferAsset* entry point. The constant is compile-time folded, so no runtime KECCAK256 preimage exists for it. The fixture has `"other_mapping_slots": [2,3,4,6,7]` and no `other_slots` key (MarketConfig.other_slots defaults to empty, lib.rs:150-151). Routing in lib.rs:543-551: `} else if market.other_slots.contains(&r.key) || market.other_mapping_slots.iter().any(|base| mapping_has_base(r.key, &preimages, base)) { } else { return Err(Error::msg(format!("unresolved storage for Comet 0x{} at key 0x{}; refusing incomplete balance state", ...))) }`. keccak256("comet.reentrancy.guard") = 0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac (library validated against keccak256("") = c5d24601...5a470).
  - Failure scenario: Any mainnet block containing one successful cUSDCv3 `supply`, `withdraw`, `transfer` or `buyCollateral` call: geth's SetState hook fires OnStorageChange twice for the guard slot (0->1 then 1->0, neither is a no-op so common/persist keeps both); `reduce` merges them into one Reduced {old: 0, new: 0, count: 2} for key 0xc98c...53ac; the key is not indices/totals/implementation, has no preimage, is not in other_slots, and no mapping base matches -> `project` returns Err("unresolved storage ...") and the whole block (all holder principals and index rows of that block) fails closed. With the pinned source, this is essentially every block with Comet user activity.
  - Suggested fix: Add `"other_slots": ["0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac"]` to the fixture (and document it in README as the reviewed reentrancy-guard slot from CometCore.sol:60), add a synthetic test that a 0->1 then 1->0 write pair on that slot in one Comet call is accepted and yields no rows, and consider asserting in `parse` that a bound market whose source_pin includes the reentrancy guard has this slot reviewed.
  - Applies to siblings: compound-v2 reviews `_notEntered` (slot 0) and lido reviews the Aragon mutex by name; erc4626 models have no guard. Re-check each crate for any other slot written on every call.
- **[medium]** `compound-v3/balance-state/src/lib.rs:502` (contract-deviation)  
  Totals-word rows are dropped when the decoded field did not change, contradicting the balance_state contract (one row per decoded field of a written packed slot; END_OF_BLOCK always emitted for every key written) and leaving consumers unable to initialise `totalSupplyBase`/`totalBorrowBase`/`lastAccrualTime` from the first observed write.
  - Evidence: lib.rs:501-504: `let row = field_row(config, market, &r, Field { field, offset, width, scale }); if row.value != row.previous_value { events.global_state.push(row); }` while the indices branch (lib.rs:479-492) pushes both index rows unconditionally. proto/v1/balance_state.proto GlobalState doc: "One persisted write to a packed slot yields one row per decoded field (same slot and raw words, different bit ranges)." and BOUNDARY_END_OF_BLOCK: "Always emitted for every key written in the block". The sibling aave/balance-state `field_row` (aave/balance-state/src/lib.rs:448-459) emits every decoded field unconditionally. Pinned source: `withdrawBase` changes only `totalBorrowBase` (+ `lastAccrualTime` via accrueInternal) while `totalSupplyBase` stays constant; `pause` (CometWithExtendedAssetList.sol:496-517) writes only `pauseFlags`.
  - Failure scenario: Epoch activated at block N (no snapshot is emitted, only observed writes). The first Comet write after N is a borrower's `withdraw(USDC)`: the map emits COMET_TOTAL_BORROW_BASE and COMET_LAST_ACCRUAL_TIME but no COMET_TOTAL_SUPPLY_BASE row because bits 0..104 were unchanged. A consumer following conformance::comet cannot compute `utilization()` (needs total_supply_base), hence cannot compute `accrued_indices` or `balance_of` for ANY supplier at any timestamp after last_accrual_time, even though the persisted totals word (raw_word) carried the value. README line 'only fields that changed' documents the deviation rather than the contract.
  - Suggested fix: Push all four totals rows for every reduced write of `totals_slot` (mirror the indices branch); consumers that want change-only semantics can compare `value` with `previous_value`. Update tests.rs:136-158 (`market_words_split_into_indices_totals_clock_and_flags`) to expect 6 rows and the README table accordingly.
  - Applies to siblings: lido filters unchanged low/high halves of the packed words and erc4626 filters unchanged Aave reserve fields the same way; the proto says one row per decoded field of a written slot. Decide once and apply to all four crates.
- **[medium]** `compound-v3/balance-state/src/lib.rs:515` (contract-deviation)  
  A persisted write of a holder's `userBasic` word that leaves `principal` unchanged emits no HolderBasis row, contradicting the contract ('a row is emitted for a holder whose basis word was written in this block') and the sibling packages, so holders whose word is written only by accrual/assetsIn updates after activation stay uninitialised for the consumer.
  - Evidence: lib.rs:511-517: `let value = signed_bits(&r.new, 0, 104); let previous = signed_bits(&r.old, 0, 104); // Tracking fields in the same word can change without the principal; an unchanged principal is not a holder update. if value == previous { continue; }`. proto/v1/balance_state.proto HolderBasis doc: "A row is emitted only for a holder whose basis word was written in this block. A holder without a row is missing/uninitialized". aave/balance-state/src/lib.rs:563-590 emits the holder row for every written user-state word (whose high bits also change without the balance). Pinned source writes the whole word with unchanged principal in `accrueAccount` (CometWithExtendedAssetList.sol:287-292: `updateBasePrincipal(account, basic, basic.principal)`), `updateAssetsIn` (:604-630: `userBasic[account].assetsIn |= ...`) on every collateral supply/withdraw, and `transferInternal` for collateral assets.
  - Failure scenario: Holder H supplied 1,000 USDC before the epoch's activation block N (principal P > 0 already in storage). After N, H only supplies WETH collateral (persisted write of H's userBasic word via `updateAssetsIn`, principal bits unchanged) and later calls `accrueAccount` (whole word rewritten, principal unchanged). The map emits no HolderBasis row for H although H's basis word was persisted-written twice with P decodable from `raw_word`; a consumer that started at N treats H as 'missing/uninitialized' indefinitely instead of learning P from the first written word. The README's 'emitted only when the principal changed' documents the deviation.
  - Suggested fix: Remove the `if value == previous { continue; }` skip (or make it a parameter defaulting to contract behaviour) so every reduced write of a verified `userBasic` key yields a HolderBasis row with `value`, `previous_value`, `raw_word`; adjust the test at tests.rs:90-92 and the README row.
  - Applies to siblings: lido filters unchanged low/high halves of the packed words and erc4626 filters unchanged Aave reserve fields the same way; the proto says one row per decoded field of a written slot. Decide once and apply to all four crates.
- **[low]** `conformance/src/comet.rs:190` (revert-behaviour)  
  `borrow_balance_of` returns a value for principal == -2^103 (int104 minimum) whereas the pinned `borrowBalanceOf` reverts, because `-principal` on int104 min overflows Solidity 0.8 checked negation.
  - Evidence: comet.rs:182-191: `if !fits_int104(principal) { return Err(...) } ... if principal.sign() != Sign::Minus { return Ok(0) } Ok(present_value_borrow(borrow_index, principal.magnitude()))` with `fits_int104` admitting -2^103 (comet.rs:28-31). Pinned CometWithExtendedAssetList.sol:1233-1237: `return principal < 0 ? presentValueBorrow(baseBorrowIndex_, unsigned104(-principal)) : 0;` — `-principal` for `int104` -2^103 is a checked arithmetic overflow (Panic 0x11) before `unsigned104` runs. `balanceOf` (:1221-1225) has no negation, so the `balance_of` acceptance of -2^103 tested at comet.rs:278 is correct; `borrow_balance_of` has no test at that bound.
  - Failure scenario: principal = -(1 << 103), market with base_borrow_index = 1e15, now = last_accrual_time: conformance returns Ok(10141204801825835211973625643008) while the contract's `borrowBalanceOf` reverts; a reference comparison at that (theoretical) boundary would disagree in direction (value vs revert).
  - Suggested fix: In `borrow_balance_of`, return `Err(Unknown::Invalid("int104 negation overflow"))` when `principal == -(BigInt::one() << 103)`, and add the case to `indices_project_with_elapsed_time_and_balances_keep_signedness`.
- **[low]** `compound-v3/balance-state/src/lib.rs:229` (metadata-consistency)  
  `parse` accepts arbitrary decimals for `base_scale`, `base_index_scale` and `factor_scale` although the pinned source fixes BASE_INDEX_SCALE = 1e15 and FACTOR_SCALE = 1e18 as compile-time constants and baseScale = 10**decimals, and the map itself hardcodes 1e15 for the index-row `scale` and `basis_scale`; inconsistent parameters produce QUALIFIED_CONSTANT rows that contradict the rows they describe.
  - Evidence: lib.rs:35-36 `const BASE_INDEX_SCALE: &str = "1000000000000000"; const FACTOR_SCALE: &str = "1000000000000000000";` used at lib.rs:489 (index row scale) and :571 (basis_scale); lib.rs:229-231 emit `(pb::StateField::CometBaseScale, decimal(&i.base_scale, ..), "1")`, `(CometBaseIndexScale, decimal(&i.base_index_scale, ..), "1")`, `(CometFactorScale, decimal(&i.factor_scale, ..), "1")` with only `decimal()` (digits-only) validation and no comparison to the constants or to `base_decimals`. Pinned CometCore.sol:51 `uint64 internal constant BASE_INDEX_SCALE = 1e15;`, :57 `uint64 internal constant FACTOR_SCALE = 1e18;`; CometWithExtendedAssetList.sol constructor `baseScale = uint64(10 ** decimals_);`.
  - Failure scenario: A params file with `"base_index_scale": "1000000000000000000"` (typo, 1e18) and `"base_decimals": 6, "base_scale": "1000000000"` is accepted; the epoch emits COMET_BASE_INDEX_SCALE = 1e18 and COMET_BASE_SCALE = 1e9 as qualified constants while every index row carries scale 1e15 and balance_decimals = 6, so a consumer trusting the constant rows scales balances by 1000x.
  - Suggested fix: In `parse`, require `base_index_scale == BASE_INDEX_SCALE`, `factor_scale == FACTOR_SCALE` and `base_scale == 10^base_decimals` (fail closed with a clear message), and add a negative test.

### Checked and found correct

- Slot inference vs pinned CometStorage.sol declaration order: structs TotalsBasic/TotalsCollateral/UserBasic/UserCollateral/LiquidatorPoints take no slot; baseSupplyIndex, baseBorrowIndex, trackingSupplyIndex, trackingBorrowIndex (4 x uint64) pack into slot 0; totalSupplyBase (uint104), totalBorrowBase (uint104), lastAccrualTime (uint40), pauseFlags (uint8) = exactly 32 bytes pack into slot 1; mappings totalsCollateral=2, isAllowed=3, userNonce=4, userBasic=5, userCollateral=6, liquidatorPoints=7. Fixture indices_slot 0, totals_slot 1, user_basic_slot 5, other_mapping_slots [2,3,4,6,7] match.
- No inherited or derived contract shifts the layout: CometConfiguration declares only structs, CometMath only functions/errors, CometCore only `constant`s (MAX_ASSETS, SECONDS_PER_YEAR, BASE_INDEX_SCALE, FACTOR_SCALE, REENTRANCY_GUARD_* ...), CometMainInterface is an abstract interface with no state, CometWithExtendedAssetList adds only `immutable`/`constant` members (governor, baseToken, rate immutables, baseScale, assetList, MAX_ASSETS_FOR_ASSET_LIST).
- Indices word bit ranges: baseSupplyIndex 0..64, baseBorrowIndex 64..128 (tracking indices 128..192, 192..256 correctly left out as reward state), width 64, scale 1e15; totals word: totalSupplyBase 0..104, totalBorrowBase 104..208, lastAccrualTime 208..248, pauseFlags 248..256 — all match Solidity packing (low-order fields at the least-significant end, big-endian word).
- UserBasic packing: int104 principal 0..104, uint64 baseTrackingIndex 104..168, uint64 baseTrackingAccrued 168..232, uint16 assetsIn 232..248, uint8 _reserved 248..256 (one slot); the map reads principal as two's complement of bits 0..104; `signed_bits` sign bit is bit 103 (byte 19, bit 7); tests round-trip int104 min/max (-2^103, 2^103-1).
- Big-endian word handling: `word()` left-pads Firehose values shorter than 32 bytes; `bits()` reads bit b from byte 31 - b/8 at bit b%8; `pack()` in tests is the exact inverse; pauseFlags 0xab at 248..256 decodes to 171.
- Mapping key derivation `keccak(pad32(address) . slot)` and the preimage filter (64 bytes, first 12 bytes zero, base == user_basic_slot) match Solidity's address-key hashing; nested `isAllowed[owner][manager]` and `userCollateral[account][asset]` resolve through `mapping_has_base` to bases 3 and 6; all mapping value structs are single-slot so no key+N slots arise.
- Extension delegate (CometExt/CometExtAssetList) writes only isAllowed (slot 3) and userNonce (slot 4) via delegatecall into Comet storage; both are reviewed mapping bases. `initializeStorage` writes only slot 0 and slot 1. `pause` writes only pauseFlags in slot 1. `absorb`, `withdrawReserves`, `buyCollateral` touch only slots 1, 2, 5, 6, 7 (plus the reentrancy slot reported above).
- conformance::mul_factor == `n * factor / FACTOR_SCALE`; present_value_supply/borrow == `uint256(principal) * index / BASE_INDEX_SCALE` (floor); principal_value_supply == `safe104(present * BASE_INDEX_SCALE / index)` (floor); principal_value_borrow == `safe104((present * BASE_INDEX_SCALE + index - 1) / index)` (ceil); safe104/safe64 modelled as Err on overflow, division by zero index as Err.
- getUtilization: present values with the STORED indices, 0 when totalSupply present value is 0, else `totalBorrow * FACTOR_SCALE / totalSupply` — matches conformance Market::utilization.
- getSupplyRate/getBorrowRate: `utilization <= kink` branch `base + mulFactor(slopeLow, utilization)`, else `base + mulFactor(slopeLow, kink) + mulFactor(slopeHigh, utilization - kink)`, wrapped in safe64 — matches RateModel::kinked including the `<=` comparison and the uint64 overflow refusal.
- accruedInterestIndices: unchanged indices when timeElapsed == 0; otherwise delta = safe64(mulFactor(index, rate * timeElapsed)) then checked uint64 `+=` — matches `bump` (delta must fit u64, sum must fit u64). `getNowInternal() - lastAccrualTime` uint40 underflow revert is modelled as Err when now < last_accrual_time.
- balanceOf: accrual is evaluated before the principal sign test (accrual reverts propagate even for zero/negative principal), then `principal > 0 ? presentValueSupply(accruedSupplyIndex, principal) : 0` with floor — matches balance_of and ModelEpoch balance_rounding = FLOOR, basis_scale = 1e15, basis 0/104 signed.
- Constructor scaling: kinks copied unscaled; slopes and bases divided by SECONDS_PER_YEAR = 31_536_000 (integer division, uint64 inputs from CometConfiguration.Configuration) — matches RateModel::from_per_year.
- Fixture immutables verified by exact integer division against pinned deployments/mainnet/usdc/configuration.json (supplySlopeLow 0.0325, supplySlopeHigh 0.4, supplyBase 0, borrowSlopeLow 0.035, borrowSlopeHigh 0.25, borrowBase 0.015 at 1e18; kinks 0.8): 1030568239, 12683916793, 0, 1109842719, 7927447995, 475646879; base_scale 1e6 for 6-decimal USDC.
- Fixture identities match pinned deployments/mainnet/usdc/roots.json (comet 0xc3d688B66703497DAA19211EEdff47f25384cdc3) and configuration.json (baseTokenAddress 0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48); implementation_slot is the EIP-1967 implementation slot 0x3608...bbc and a write to it fails closed.
- BASE_INDEX_SCALE = 1e15 and FACTOR_SCALE = 1e18 in the map and conformance equal CometCore.sol:51 and :57; SECONDS_PER_YEAR equals CometCore.sol:45.
- Persisted-effect handling reuses common/persist unchanged (reverted frames and failed transactions contribute nothing; test covers state_reverted); reduce() enforces strictly increasing ordinals and old==previous-new continuity per key; code changes on Comet or implementation fail closed.
- cargo test -p compound-v3-balance-state: 6 passed; cargo test -p conformance comet: 4 passed (pinned toolchain, offline).

## Finder `review:compound-v3:contract`

6 findings, 19 properties checked and found correct.

### Findings

- **[high]** `compound-v3/balance-state/tests/fixtures/mainnet-cusdcv3-epoch.json:19` (fail-closed-on-routine-block)  
  The committed cUSDCv3 configuration does not review Comet's reentrancy-guard slot, so every persisted supply/withdraw/transfer/buyCollateral write fails the block as 'unresolved storage'.
  - Evidence: Pinned CometCore.sol l.60: `bytes32 internal constant REENTRANCY_GUARD_FLAG_SLOT = bytes32(keccak256("comet.reentrancy.guard"));` (= 0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac). Pinned CometWithExtendedAssetList.sol l.179-201: `nonReentrantBefore` does `sstore(slot, REENTRANCY_GUARD_ENTERED)` and `nonReentrantAfter` does `sstore(slot, REENTRANCY_GUARD_NOT_ENTERED)`; l.737 `function supplyInternal(...) internal nonReentrant`, l.848 `transferInternal(...) nonReentrant`, l.959 `withdrawInternal(...) nonReentrant`, l.1122 `buyCollateral(...) nonReentrant`. Fixture l.19-25 lists only `other_mapping_slots` [2,3,4,6,7] and no `other_slots` (README l.39-41 lists slots 0, 1, 5 only). lib.rs l.543-551: a reduced Comet write that is not indices/totals/userBasic/other_slots/other_mapping_slots hits `return Err(... "unresolved storage for Comet 0x{} at key 0x{}; refusing incomplete balance state")`. common/persist l.94-96 treats 0->1 and 1->0 individually as non-no-ops, and lib.rs `reduce` (l.328-366) merges them into one Reduced(old=0,new=0,count=2) with no old==new filter. Contrast: lido/balance-state/tests/fixtures/mainnet-steth-v4-epoch.json l.37 and lido tests.rs l.351-352 explicitly review `aragonOS.reentrancyGuard.mutex` 0->1->0.
  - Failure scenario: Any successful `supply`, `withdraw`, `transfer` or `buyCollateral` transaction on cUSDCv3 (i.e. essentially every block that also changes a holder's principal) records two persisted StorageChange on key 0xc98c7730...53ac in the Comet proxy's storage; `project` reaches the `else` branch and returns Err, so the block fails, all HolderBasis/GlobalState rows of that block are lost and the stream halts. Only accrue/absorb/allow/pause-only blocks would pass.
  - Suggested fix: Add `"other_slots": ["0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac"]` to the fixture (or derive the constant in code from the pinned label like the EIP-1967 slot and always review it), document it in the README slot list, and add a test that a userBasic-writing frame with guard writes 0->1 (ordinal n) and 1->0 (ordinal n+k) is accepted, emits the HolderBasis row and no GlobalState row for the guard key.
  - Applies to siblings: compound-v2 reviews `_notEntered` (slot 0) and lido reviews the Aragon mutex by name; erc4626 models have no guard. Re-check each crate for any other slot written on every call.
- **[medium]** `compound-v3/balance-state/src/tests.rs:194` (test-coverage)  
  No test exercises the `other_slots` scalar review path nor a reduced write whose net effect is old == new (the reentrancy-guard shape), so the defect above is invisible to the synthetic suite.
  - Evidence: tests.rs l.194-254 covers reviewed mapping chains (`other_mapping_slots[1]`), unresolved key, pointer write, code change, reverted frame, tie and discontinuity, but the fixture has no `other_slots` and no test writes a scalar reviewed slot; lib.rs l.543 `market.other_slots.contains(&r.key)` is therefore never true in any test. README l.71-73 claims the tests verify 'reviewed mapping chains and every refusal above'.
  - Failure scenario: A regression in the `other_slots` branch (or the missing guard slot in the fixture) passes `cargo test -p compound-v3-balance-state`; the first real block with a supply then fails closed.
  - Suggested fix: Extend `parameters_are_explicit_and_fail_closed`/`reviewed_mappings_...` with an `other_slots` entry in the fixture and a two-write 0->1->0 case on that slot inside a frame that also writes userBasic; assert Ok, one HolderBasis row, and `global_state.is_empty()`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/lib.rs:582` (epoch-metadata)  
  `global_carryover` is hard-coded `true` for every BOUND/REAFFIRMED row although, by the package's own model, each Comet epoch change is a rate-model replacement, for which the contract defines the flag as false.
  - Evidence: lib.rs l.581-582: `basis_carryover: true, global_carryover: true,` (unconditional). lib.rs l.615-616 comment: 'Rate and scale immutables are baked into the implementation; every governance rate change is a new implementation.' README l.25-27 says the same. proto/v1/balance_state.proto l.441-443: `// Same statement for retained GlobalState rows; false at a storage migration or rate-model replacement. bool global_carryover = 24;`. lido/balance-state/src/lib.rs l.437 shows the flag is meant to be family-accurate (`global_carryover: false`).
  - Failure scenario: An operator binds epoch 2 for cUSDCv3 at block N after governance deploys a new implementation with new kinks/slopes. The epoch-2 BOUND row states `global_carryover = true`, telling a consumer that retained epoch-1 GlobalState rows (including the epoch-1 QUALIFIED_CONSTANT kink/slope rows) remain evaluable under epoch 2, which contradicts the proto definition and can be used to project accrued indices with stale rate immutables if the consumer honors the flag over the re-declared constants.
  - Suggested fix: Make `basis_carryover`/`global_carryover` per-epoch parameters in `MarketConfig` (validated, no default) or set `global_carryover: false` whenever `epoch > 1`, and state in the README that only observed-write rows carry across a Comet epoch while constants are re-declared on BOUND.
  - Applies to siblings: compound-v2, lido and erc4626 hard-code carryover flags too (lido sets global_carryover=false).
- **[medium]** `compound-v3/balance-state/src/lib.rs:188` (acceptance)  
  `producer_versions` accepts any positive value, so a parameter such as `[3]` makes the map accept Extended v3 blocks that the contract says are refused.
  - Evidence: lib.rs l.187-190: `require(!raw.producer_versions.is_empty() && raw.producer_versions.iter().all(|v| *v > 0), "qualified producer versions required")` and l.394 `require(config.producer_versions.contains(&block.ver), ...)`. docs/balance-state-contract.md l.75-78: 'reduced in execution-ordinal order (producer versions 4 and 5 only; version 3 is refused)'. proto l.367: `// sf.ethereum.type.v2.Block.ver; the package accepts 4 and 5 only.` (Same pattern in aave/lido/erc4626/compound-v2 packages.)
  - Failure scenario: Parameters `{"chain_id":1,"producer_versions":[3],...}` with a v3 Extended block: the block passes `validate_block`, rows are emitted with `ordinal`/`first_ordinal`/`change_count` reduced over v3 ordinals the contract declares unqualified, and `BlockClock.producer_version = 3` contradicts the proto comment.
  - Suggested fix: In `parse`, require every listed version to be 4 or 5 (`require(raw.producer_versions.iter().all(|v| matches!(v, 4 | 5)), ...)`), or amend the contract doc/proto comment if operator-declared versions are intended; apply the same choice to the sibling packages.
  - Applies to siblings: every new crate accepts any positive producer version; aave/balance-state and native/balances too.
- **[low]** `compound-v3/balance-state/src/lib.rs:230` (parameter-validation)  
  The echoed `base_index_scale`, `factor_scale` and `base_scale` parameters are emitted as QUALIFIED_CONSTANT rows without being cross-checked against the hard-coded scale strings the same block uses, so an inconsistent configuration is accepted silently.
  - Evidence: lib.rs l.35-36 hard-code `BASE_INDEX_SCALE = "1000000000000000"` and `FACTOR_SCALE = "1000000000000000000"`, used for index-row `scale` (l.489), constant `scale` (l.197-228) and `ModelEpoch.basis_scale` (l.571); l.229-231 emit `decimal(&i.base_scale)`, `decimal(&i.base_index_scale)`, `decimal(&i.factor_scale)` with only a digits check. Pinned CometCore.sol l.51/57: `BASE_INDEX_SCALE = 1e15`, `FACTOR_SCALE = 1e18` are source constants, and `baseScale = 10 ** decimals` (CometWithExtendedAssetList.sol constructor) must equal 10^base_decimals.
  - Failure scenario: An operator sets `"base_index_scale": "100000000000000"` (one zero short) or `"base_scale": "1000000"` with `"base_decimals": 18`; `parse` accepts it and the block carries a COMET_BASE_INDEX_SCALE row saying 1e14 next to index rows and a ModelEpoch saying 1e15, or a base scale inconsistent with `balance_decimals`, with no refusal.
  - Suggested fix: In `parse`, `require(i.base_index_scale == BASE_INDEX_SCALE && i.factor_scale == FACTOR_SCALE, ...)` and `require(i.base_scale == 10^base_decimals as string, ...)`, so the echoed constants can only agree with the emitted scales.
  - Applies to siblings: compound-v2 and erc4626 also echo caller-supplied constants without cross-checks.
- **[low]** `compound-v3/balance-state/README.md:27` (docs)  
  The README describes the uncarried part of the user word as 'bits 104..168', but the pinned UserBasic struct has three more non-principal fields up to bit 256.
  - Evidence: README l.27-28: 'Tracking indices (bits 128..256 of the indices word and bits 104..168 of the user word) are reward state and are not carried.' Pinned CometStorage.sol l.29-35: `struct UserBasic { int104 principal; uint64 baseTrackingIndex; uint64 baseTrackingAccrued; uint16 assetsIn; uint8 _reserved; }` i.e. baseTrackingIndex 104..168, baseTrackingAccrued 168..232, assetsIn 232..248, _reserved 248..256. lib.rs l.511-517 correctly ignores everything above bit 104.
  - Failure scenario: A reader auditing the HolderBasis provenance concludes bits 168..256 are carried or unused; a write that changes only `assetsIn` (collateral entered/exited) produces no row, which the README does not explain.
  - Suggested fix: Change the sentence to 'bits 104..256 of the user word (baseTrackingIndex, baseTrackingAccrued, assetsIn, _reserved)'.

### Checked and found correct

- Slot inference matches the pinned CometStorage.sol declaration order: eight packed scalars occupy slots 0 (baseSupplyIndex|baseBorrowIndex|trackingSupplyIndex|trackingBorrowIndex) and 1 (totalSupplyBase|totalBorrowBase|lastAccrualTime|pauseFlags); mappings totalsCollateral=2, isAllowed=3, userNonce=4, userBasic=5, userCollateral=6, liquidatorPoints=7; CometConfiguration and CometMath declare no state, CometMainInterface/CometWithExtendedAssetList declare only immutables, so no base-contract slot shift exists.
- Every mapped struct (TotalsCollateral, UserBasic, UserCollateral, LiquidatorPoints) packs into exactly 256 bits, so mapping entries are single slots and no key+n offsets are needed by mapping_has_base.
- Bit ranges emitted match the layout: baseSupplyIndex 0..64 and baseBorrowIndex 64..128 (scale 1e15); totalSupplyBase 0..104, totalBorrowBase 104..208, lastAccrualTime 208..248, pauseFlags 248..256 (scale 1); UserBasic.principal int104 at 0..104 with signed=true; bit_offset/bit_width set on every observed row.
- signed_bits reads the sign bit at offset+width-1 and subtracts 2^width; int104 extremes (-2^103, 2^103-1) and the negative-principal-keeps-sign rule are tested and match BASIS_KIND_SIGNED_PRINCIPAL semantics (never clamped).
- conformance::comet reproduces the pinned bodies: presentValueSupply/Borrow floor, principalValueBorrow ceil, getUtilization from STORED indices with zero-supply guard, kinked getSupplyRate/getBorrowRate with `utilization <= kink`, accruedInterestIndices with safe64 on the delta and a checked uint64 add, balanceOf = principal > 0 ? presentValueSupply(accrued) : 0, borrowBalanceOf = principal < 0 ? presentValueBorrow(accrued, -principal) : 0, now < lastAccrualTime rejected like the uint40 subtraction revert.
- Constructor scaling `perYear / SECONDS_PER_YEAR` (31,536,000) matches the pinned constructor; the fixture immutables (1030568239, 12683916793, 0, 1109842719, 7927447995, 475646879, kinks 0.8e18) equal the pinned deployments/mainnet/usdc/configuration.json rates after that scaling, and comet/base_token addresses match roots.json/configuration.json.
- EIP-1967 implementation slot 0x360894a1...382bbc is correct; a persisted write to it fails the block before any decoding; Dependency.pointer_value is the 32-byte left-padded implementation address as a TransparentUpgradeableProxy stores it.
- Code changes on the bound Comet proxy or its implementation (from Block.code_changes, system calls, transactions and EIP-7702 authority code) fail the block; base token code changes are not checked, consistent with its BINDING_KIND_DECLARED edge.
- '' vs '0': observed HolderBasis/GlobalState rows always carry decoded value and previous_value ('0' for a zero word); QUALIFIED_CONSTANT rows leave previous_value, key, storage_slot and raw words empty and set storage_contract to the implementation as the proto requires; Comet scalar fields carry key ''.
- Enum usage: OBSERVED_WRITE + END_OF_BLOCK + TRANSACTION/SYSTEM_CALL for writes; QUALIFIED_CONSTANT + DECLARATION + EPOCH for constants; BOUND at activation_block, REAFFIRMED on (number - activation) % heartbeat == 0; MODEL_FAMILY_COMPOUND_V3_COMET, BASIS_KIND_SIGNED_PRINCIPAL, ROUNDING_FLOOR; no *_UNSPECIFIED value is ever emitted.
- ModelEpoch basis_* fields: basis_scale '1000000000000000', basis_bit_offset 0, basis_bit_width 104, basis_signed true, balance_asset = base token, balance_decimals = base_decimals, implementation and implementation_slot filled, scope EPOCH, implementation_revision '' (Comet declares no numeric revision), INVALIDATED/SUSPENDED never emitted because live pointer/code surprises are module errors as docs/balance-state-contract.md l.72-73 allows.
- Dependency rows: IMPLEMENTATION (depth 1, parent '', STORAGE_POINTER with pointer_contract/pointer_slot/pointer_value) and UNDERLYING (depth 1, DECLARED), each with activation_block and source_pin, emitted with both BOUND and REAFFIRMED kinds.
- Persistence: only common/persist-persisted writes are consumed (reverted frames and failed transactions tested); reduce requires ordinal > 0, strictly increasing ordinals per key ('ambiguous'), old == previous new ('discontinuous'), and sets first_ordinal/ordinal/change_count with the last write's tx provenance.
- Keccak preimages are recomputed and checked against the key; a userBasic key must have 12 zero bytes + 20-byte address + user_basic_slot; nested mapping chains (isAllowed[owner][manager], userCollateral[account][asset]) resolve to their reviewed base with cycle protection; delegatecall frames (implementation, CometExt) write the proxy's storage so `address == comet` filtering is correct.
- BlockClock: exactly one per block, also with no markets, no active markets, or no epochs; counts computed after sorting; timestamp, hash, parent_hash, state_root, producer_version, spec_revision, package, package_version, parameters_sha256 (SHA-256 of the exact params string) filled; alias_count 0.
- Sort order is deterministic; the code's sort keys omit `depth`, `boundary` and `ordinal/kind` relative to the proto comments but every natural key has at most one row per block for this package, so the emitted order is identical.
- Rows are flat: no nested messages, repeated fields, oneof or optional inside any row; Events has only top-level repeated tables, satisfying the sink mapping section.
- Parameters fail closed as the README table states: deny_unknown_fields, non-decimal immutables, overlapping scalar slots, other_slots colliding with scalars, other_mapping_slots containing user_basic_slot, duplicate market, empty producer versions, non-positive epoch/activation_block.
- cargo test -p compound-v3-balance-state (6 tests) and cargo clippy -p compound-v3-balance-state --all-targets -- -D warnings pass on the pinned toolchain.

## Finder `review:compound-v3:tests`

12 findings, 15 properties checked and found correct.

### Findings

- **[high]** `compound-v3/balance-state/tests/fixtures/mainnet-cusdcv3-epoch.json:19` (test-coverage)  
  The committed cUSDCv3 epoch reviews no scalar slots, so the reentrancy-guard slot that the pinned implementation writes on every supply/withdraw/transfer/buyCollateral is 'unresolved' and every routine block fails closed; no test exercises a realistic transaction shape.
  - Evidence: Fixture has `other_mapping_slots` [2,3,4,6,7] but no `other_slots` key at all. Pinned CometCore.sol:60 `bytes32 internal constant REENTRANCY_GUARD_FLAG_SLOT = bytes32(keccak256("comet.reentrancy.guard"));` and CometWithExtendedAssetList.sol:179-201 `nonReentrantBefore(){... assembly { sstore(slot, REENTRANCY_GUARD_ENTERED) } }` / `nonReentrantAfter(){ assembly { sstore(slot, REENTRANCY_GUARD_NOT_ENTERED) } }`, applied by `nonReentrant` on `supplyInternal` (:737), `transferInternal` (:848), `withdrawInternal` (:959), `buyCollateral` (:1122). lib.rs:543-551 routes any key that is not indices/totals/userBasic/other_slots/other_mapping_slots to `Err("unresolved storage ...")`. Scratch reproduction against the crate (call with guard 0->1 @10, userBasic write @11, guard 1->0 @12) returned: `ERR unresolved storage for Comet 0xc3d688b6...cdc3 at key 0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac; refusing incomplete balance state`. The Lido fixture lists `aragonOS.reentrancyGuard.mutex` for exactly this reason (lido/.../mainnet-steth-v4-epoch.json:37); compound-v2 lib.rs:749 reviews its 'reentrancy flag'. tests.rs never builds a call containing a guard write; every test writes only the balance slots in isolation.
  - Failure scenario: Any real cUSDCv3 block containing a single supply(), withdraw(), transfer() or buyCollateral() -> map_events returns an error and the stream halts, even though every balance-relevant write is fully described. The suite passes because no test contains the two guard SSTOREs (0->1 then 1->0, each a non-noop record under common/persist), so the omission is invisible offline.
  - Suggested fix: Add `"other_slots": ["0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac"]` (= keccak256("comet.reentrancy.guard")) to the fixture and document its derivation in README/source_pin, or (as lido/balance-state does) accept `other_slot_names` and hash them in `parse`. Add a test `routine_supply_resolves_guard_indices_totals_and_principal` that builds one call with guard 0->1 (ordinal 10), indices write (11), totals write (12), userBasic 0->5_000_000 (13), guard 1->0 (14) and asserts `project` is Ok, one holder row, the expected global rows, and no row for the guard slot; also assert `keccak(b"comet.reentrancy.guard") == market.other_slots[0]` so the fixture cannot silently drift.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/tests.rs:239` (test-coverage)  
  The comment claims 'Reverted frames and failed transactions emit nothing' but only a reverted frame inside a SUCCEEDED transaction is tested; no FAILED/REVERTED transaction (status 2/3) reaches `project`.
  - Evidence: tests.rs:239-243 sets `call.state_reverted = true` on a call inside `tx(...)` whose status is hard-coded `Succeeded` (tests.rs:30). No test constructs `status: eth::TransactionTraceStatus::Reverted/Failed`. README.md:59 states 'Reverted frames and failed transactions never contribute' and :71-73 claims the tests verify 'every refusal above'. lib.rs:404 also requires `(1..=3).contains(&tx.status) && !tx.calls.is_empty()`, untested. Scratch run of a Reverted tx whose root call writes userBasic 1->2 gave `holder rows = 0, global = 0` (correct today, but unpinned).
  - Failure scenario: A regression that bypasses `persist::collect_transaction` for the root call of a failed tx (or a change in common/persist's failed-tx rule) would turn a reverted borrow's userBasic write into a HolderBasis row with a wrong principal, and the suite would still pass.
  - Suggested fix: Add a test: Reverted tx (status 3) whose root call writes userBasic 1->2 and the totals word -> Ok with no holder/global rows and `clocks[0].holder_basis_count == 0`; a Failed tx (status 2) with empty `calls` and a tx with `status: 0` -> Err containing 'incomplete transaction persistence data'.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/tests.rs:244` (test-coverage)  
  Only the tie and discontinuity refusals of `reduce` are tested; a valid same-block repeated write (the BOUNDARY_END_OF_BLOCK contract) is never asserted, so previous_value/value/change_count/first_ordinal/ordinal and last-write provenance are unpinned.
  - Evidence: tests.rs:244-253 asserts only `discontinuous` and `ambiguous`. lib.rs:334-344 mutates `r.new`, `r.ordinal`, `r.count`, `r.tx_hash`, `r.tx_index`, `r.call_index`, `r.scope` on the second write; proto/v1/balance_state.proto:127-131 defines END_OF_BLOCK as 'previous_value is the value before the first write, first_ordinal its ordinal, ordinal the last write's ordinal'. Scratch run (writes 1->2 @10 in tx 9, 2->-3 @20 in tx 10) produced `prev=1 value=-3 count=2 first=10 ord=20 txidx=10` — correct but no test would catch a regression (e.g., swapping `r.old = w.old`).
  - Failure scenario: A regression that resets `old` on the second write, or keeps the first write's tx provenance, yields a HolderBasis whose `previous_value` is the intra-block value (2) instead of 1 or whose transaction_hash points at the wrong transaction; consumers computing deltas or auditing provenance get wrong data while all six tests pass.
  - Suggested fix: Add a test with two transactions writing the same holder (1->2 @10 in tx index 9, 2->-3 @20 in tx index 10, different tx hashes) and assert `(previous_value, value, change_count, first_ordinal, ordinal, transaction_index, transaction_hash) == ("1", "-3", 2, 10, 20, 10, [8;32])`; add the same for the totals word across two calls and assert `clocks[0].holder_basis_count == 1`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `conformance/src/comet.rs:248` (test-coverage)  
  The above-kink rate branch is asserted only by inequality (`below < at_kink < above`), which a wrong high-slope branch also satisfies; the borrow above-kink branch is not exercised at all.
  - Evidence: comet.rs:245-249: `let above = m.rates.supply_rate(&BigUint::from(900_000_000_000_000_000u64)).unwrap(); assert!(below < at_kink && at_kink < above); assert_eq!(at_kink as u128, 1_030_568_239u128 * 800_000_000_000_000_000u128 / FACTOR_SCALE as u128);` — `at_kink` takes the `utilization <= kink` branch (comet.rs:97), so the `else` branch (line 100) is only checked for being larger. Pinned getSupplyRate: `safe64(base + mulFactor(slopeLow, supplyKink) + mulFactor(slopeHigh, (utilization - supplyKink)))`. With the fixture immutables the exact value at 0.9e18 is 824_454_591 + 1_268_391_679 = 2_092_846_270; a bug using `low` instead of `high` in the else branch yields 927_511_414, which still passes the inequality. `borrow_rate` is only tested at utilization 0 (comet.rs:250).
  - Failure scenario: Replace `mul_factor(&u(high), &(utilization - u(kink)))` by `mul_factor(&u(low), ...)` or drop the `- u(kink)` term: `kinked_rates_and_utilization_follow_the_stored_indices` still passes, and every projected balanceOf above 80% utilization (routine for cUSDCv3) is wrong.
  - Suggested fix: Assert exact values: `assert_eq!(m.rates.supply_rate(&0.9e18).unwrap(), 2_092_846_270)` and `assert_eq!(m.rates.borrow_rate(&0.9e18).unwrap(), 2_156_265_853)` (= 475_646_879 + 887_874_175 + 792_744_799), plus one point just above the kink (kink+1) to pin the `<=` comparator against the pinned `if (utilization <= supplyKink)`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `conformance/src/comet.rs:264` (test-coverage)  
  'Long elapsed' and uint64-limit acceptance items are not covered: index projection is asserted only as `s1 > s0` for 3600 s, and none of the three checked-cast error paths in `accrued_indices` (`rate exceeds uint64`, `index delta exceeds uint64`, `index exceeds uint64`) is exercised.
  - Evidence: comet.rs:263-264 `let (s1, b1) = m.accrued_indices(m.last_accrual_time + 3600).unwrap(); assert!(s1 > s0 && b1 > b0);`. Error arms at comet.rs:102-104, 153-155, 157-159 have no test. Issue #15 acceptance: 'equal/long elapsed timestamps ... uint64/int104 limits'. Pinned `accruedInterestIndices`: `baseSupplyIndex_ += safe64(mulFactor(baseSupplyIndex_, supplyRate * timeElapsed));` where `safe64` reverts above 2^64-1 and the uint64 `+=` is checked in 0.8.15. Expected exact values for the test market (utilization 812750275760136302, supply rate 986_178_027, borrow rate 1_464_598_202): 3600 s -> (1_058_127_213_382_182, 1_074_993_322_251_046); 31_536_000 s -> (1_091_031_212_963_283, 1_124_638_720_669_845).
  - Failure scenario: An off-by-scale regression in `bump` (e.g., dividing by BASE_INDEX_SCALE instead of FACTOR_SCALE, or multiplying rate by elapsed after the division) keeps `s1 > s0` true and the suite green while every projected balance in idle blocks is wrong; an overflow that Solidity would revert on is silently wrapped or accepted.
  - Suggested fix: Assert the exact pairs above for 3600 s and 31_536_000 s; add `Market { base_supply_index: u64::MAX, ..market() }.accrued_indices(last + 1)` -> `Err(Unknown::Invalid("index exceeds uint64"))`, a rate model with `supply_slope_low: u64::MAX` at utilization 1e18 -> `Err("rate exceeds uint64")`, and a market whose `mul_factor(index, rate*elapsed)` exceeds 2^64 -> `Err("index delta exceeds uint64")`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/lib.rs:450` (test-coverage)  
  No test covers a block before `activation_block`: the fixture activates at block 1 and `validate_block` rejects block 0, so the 'inactive market ignores every Comet write, including unresolved keys, and emits only the clock' path is never executed.
  - Evidence: lib.rs:450 `let active: Vec<&Market> = config.markets.iter().filter(|m| m.activation_block <= block.number).collect();` and :452 `if !active.is_empty() { ... }`. Fixture `activation_block: 1`; tests.rs:12-27 build blocks 1, 2, 10, 1001 only; lib.rs:400 `block.number > 0` makes block 0 unusable. Issue #15 acceptance: 'Do not infer an older deployment's behavior from current source' — the pre-activation era must be skipped, not decoded or refused.
  - Failure scenario: A regression that drops the `activation_block <= block.number` filter (or the `!active.is_empty()` guard) would decode pre-epoch cUSDCv3 writes under the wrong epoch or fail every pre-activation block with 'unresolved storage'; with the real epoch bound at a 2024+ block this is the first thing a backfill hits, and the suite would not notice.
  - Suggested fix: Add a test that mutates the fixture JSON (`activation_block = 100`) and projects block 50 containing a write to an unknown key (`[0x99;32]`) and a userBasic write at the Comet address: expect Ok, `epochs`/`holder_basis`/`global_state` empty and exactly one clock with all counts 0; then project block 100 and assert `EpochEventKind::Bound`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/lib.rs:389` (test-coverage)  
  Of the seven `validate_block` refusals only the producer-version check is tested; README claims every refusal is verified.
  - Evidence: lib.rs:390-405 requires Extended detail level, listed `ver`, present header, 32-byte hash/parent/state_root, `header.number == block.number && block.number > 0`, present non-negative timestamp, and tx status 1..=3 with non-empty calls. tests.rs:269-271 only sets `b.ver = 4`. README.md:52 lists 'Non-Extended block, unlisted Block.ver, incomplete transaction data | block fails' and :71-73 says the tests verify 'every refusal above'.
  - Failure scenario: A Base (non-Extended) block or a block whose header number disagrees with `block.number` is accepted after a refactor that reorders/loses one `require`; the map then emits a BlockClock with a wrong number/timestamp that consumers use as the evaluation clock for every Comet balance in that block.
  - Suggested fix: Add a table-driven test: `detail_level = DetaillevelBase` -> 'Extended blocks required'; `header = None` -> 'missing header'; `header.number = number + 1` -> 'header number mismatch'; `hash = vec![1; 31]` -> 'invalid block identity'; `timestamp = None` -> 'missing timestamp'; tx with `status: 0` or `calls: vec![]` -> 'incomplete transaction persistence data'; and assert the clock-only block still emits exactly one BlockClock with `producer_version == 5`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/tests.rs:80` (test-coverage)  
  The 'positive/zero/negative principal' acceptance item is only partly covered: no holder row with value "0" (full withdrawal) and no 0 -> negative first-borrow crossing are asserted, so the '"0" is a known zero, "" is not carried' contract is unpinned for the most common repayment/withdrawal shapes.
  - Evidence: tests.rs:80-102 tests 5_000_000 -> -2_500_000, a tracking-only write, and int104 extremes. proto/v1/balance_state.proto:524 'A holder without a row is missing/uninitialized; "0" is a known zero.' lib.rs:511-517 emits the row whenever `value != previous`, so 5_000_000 -> 0 must yield `value == "0"` and 0 -> -100_000_000 must yield `previous_value == "0"`, `value == "-100000000"`; neither is asserted. Zero-as-a-row is exactly the case where a consumer distinguishes 'withdrawn' from 'never seen'.
  - Failure scenario: A regression that treats a zero decoded principal like the unchanged case (e.g., `if value.is_zero() || value == previous { continue }` introduced while filtering dust) drops the full-withdrawal row; the consumer keeps the stale 5_000_000 principal and reports a balance for an account that has 0 on chain. The current suite passes.
  - Suggested fix: Extend the signed-principal test with: 5_000_000 -> 0 asserting `(previous_value, value) == ("5000000", "0")` and `holder_basis_count == 1`; 0 -> -100_000_000 asserting `("0", "-100000000")`; and a first-time supplier whose old word is all zeros (also with `old_value: vec![]` as Firehose may encode a zero word) asserting previous_value "0".
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[low]** `compound-v3/balance-state/src/lib.rs:480` (test-coverage)  
  An indices-word write that changes only the tracking indices emits two base-index rows whose value equals previous_value, inconsistent with the totals-word 'only fields that changed' filter, and this behavior plus a pauseFlags change are untested.
  - Evidence: lib.rs:480-492 pushes CometBaseSupplyIndex/CometBaseBorrowIndex unconditionally; lib.rs:501-504 filters totals fields with `if row.value != row.previous_value`. Scratch run with a word changing only bits 128..192 produced `global rows = 2 (values [(30, "1000000000000000", "1000000000000000"), (31, ...same...)])`. Pinned `accrueInternal` (CometWithExtendedAssetList.sol:271-283) updates tracking indices in the same word as the base indices; with `supplyPerSecondInterestRateBase = 0` (fixture `supply_rate_base: "0"`) and zero utilization the base supply index does not move while the borrow index and tracking indices can. No test writes a nonzero pauseFlags value (tests.rs:114/120 both use 0) although lib.rs:499 decodes it.
  - Failure scenario: Consumers that count GlobalState rows as 'index moved' events (or diff `value` vs `previous_value` to compute accrual) see phantom unchanged rows for the indices word but never for the totals word; a regression flipping the comparator in the totals filter would not be caught either since only changed totals fields are asserted.
  - Suggested fix: Decide and pin: either apply the same `value != previous_value` filter to the indices fields and assert that a tracking-only write yields no rows, or keep unconditional emission and assert the two unchanged rows explicitly and document it in README's table. Add a totals-word test where only bits 248..256 change (0 -> 7 = supply|transfer|withdraw paused) asserting a single CometPauseFlags row with `(previous_value, value, bit_offset, bit_width) == ("0", "7", 248, 8)`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[low]** `compound-v3/balance-state/src/tests.rs:1` (test-coverage)  
  The 'deterministic rollback/replay' acceptance item has no test: output is never shown to be byte-identical across re-runs or invariant to the input array order of storage changes / transactions.
  - Evidence: Issue #15: 'Add Rust fixtures for ... deterministic rollback/replay.' lib.rs:329 sorts writes by ordinal and lib.rs:634-643 sorts every output list, but no test encodes `project(...)` twice or permutes `storage_changes`/`transaction_traces` and compares. proto/v1/balance_state.proto:37-39: 'Order inside each list is deterministic ... because the sink's `_row_id_` is the element index.'
  - Failure scenario: A refactor that removes the final `holder_basis.sort_by` (BTreeMap order in `reduce` already looks sorted by (address,key)) would still pass all six tests, yet two holders' rows could swap order when the transaction order differs between a live and a replayed block, changing `_row_id_` and breaking the sink's replacing-merge semantics on reorg replay.
  - Suggested fix: Add a test that builds a block with two holders and two global words written in two transactions, projects it, then projects a copy with `transaction_traces` reversed and each call's `storage_changes` reversed, and asserts `prost::Message::encode_to_vec` of both outputs is identical and that holder rows are ordered by (market, holder).
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[low]** `compound-v3/balance-state/src/lib.rs:461` (test-coverage)  
  The delegatecall frame shape of the real proxy (Call.address = implementation, StorageChange.address = Comet) and the SYSTEM_CALL scope path are untested; every test uses `address: m.comet` on the call itself.
  - Evidence: lib.rs:462-465 gathers preimages when `call.address == m.comet || call.storage_changes.iter().any(|c| c.address == m.comet)`; the second disjunct exists for delegatecall frames but no test exercises it (tests.rs:59-72 `user_basic_call` sets `address: m.comet.clone()`). lib.rs:367-373 `scope_of` maps SystemCall/Block; `block.system_calls` is never populated in tests. Scratch run of a system call writing the totals word returned `global rows = 1 scope = [2]` (correct, unpinned).
  - Failure scenario: Simplifying the preimage condition to `call.address == m.comet` passes the suite but on the real TransparentUpgradeableProxy every userBasic write lands in a frame whose `address` is the implementation, so no preimage is found and every supply block fails with 'unresolved storage'.
  - Suggested fix: Add a test where the call has `address: m.implementation.clone()`, `call_type: Delegate`, keccak preimages for the holder, and storage changes at `m.comet`; assert one holder row with `storage_contract == m.comet`. Add a system-call test asserting `scope == pb::Scope::SystemCall` and an empty `transaction_hash`.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[low]** `compound-v3/balance-state/src/lib.rs:257` (test-coverage)  
  Multi-market blocks and the `duplicate market` parameter refusal are untested, although Ethereum has several Comets (cUSDCv3, cWETHv3) sharing identical slot numbers, so preimage lookups are keyed by hash across markets.
  - Evidence: lib.rs:257 `require(markets.iter().all(|other| other.comet != market.comet), "duplicate market")` has no test (tests.rs:257-272 covers other parameter errors only). lib.rs:460-474 builds one `preimages` map keyed by hash for all active markets; lib.rs:477 resolves the market by `r.address`. docs/extraction-coverage.md:221-222 lists cUSDCv3 and cWETHv3 on mainnet.
  - Failure scenario: A regression that resolves the market from the preimage/slot instead of `r.address`, or that attributes a cWETHv3 userBasic write (same slot 5, different contract) to the first market, would emit HolderBasis rows under the wrong `market`/`epoch`/`balance_asset` (6 vs 18 decimals) and the suite would pass.
  - Suggested fix: Add a fixture variant with two markets (cUSDCv3 and cWETHv3 `0xA17581A9E3356d9A858b789D68B4d866e593aE94`, WETH base, decimals 18, distinct placeholder implementations) and a test where both receive a userBasic write for the same holder in one block; assert two holder rows with the correct `market`, `epoch`, sort order by market, and two BOUND epochs at activation; assert that duplicating the comet address in `markets` fails parse with 'duplicate market'.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.

### Checked and found correct

- Slot inference matches the pinned CometStorage.sol declaration order: slot 0 = baseSupplyIndex|baseBorrowIndex|trackingSupplyIndex|trackingBorrowIndex, slot 1 = totalSupplyBase|totalBorrowBase|lastAccrualTime|pauseFlags, mappings 2 totalsCollateral, 3 isAllowed, 4 userNonce, 5 userBasic, 6 userCollateral, 7 liquidatorPoints; CometConfiguration, CometMath, CometMainInterface, CometExtInterface and CometExt declare no state variables (CometExt name32/symbol32 are immutable), so nothing shifts the layout (declaration-order check only, not a compiler layout).
- Bit ranges in lib.rs (indices 0..64 / 64..128; totals 0..104 / 104..208 / 208..248 / 248..256; UserBasic principal int104 at 0..104) match the pinned struct field types and Solidity low-to-high packing; tracking fields at 104..168 / 168..232 / 232..248 are correctly ignored for holder updates.
- Fixture immutables equal the pinned deployments/mainnet/usdc/configuration.json rates scaled by the pinned constructor (`perYear / SECONDS_PER_YEAR`, integer division): 0.0325e18/31536000 = 1030568239, 0.4e18 -> 12683916793, 0 -> 0, 0.035e18 -> 1109842719, 0.25e18 -> 7927447995, 0.015e18 -> 475646879; kinks 0.8e18; base_scale 1e6 = 10^6 for USDC.
- Fixture Comet 0xc3d688B66703497DAA19211EEdff47f25384cdc3 and base token USDC 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48 match deployments/mainnet/usdc/roots.json and configuration.json at the pin; base_decimals 6 is correct.
- conformance::comet present_value_supply/present_value_borrow (floor), principal_value_supply (floor) and principal_value_borrow (ceil via +index-1) match pinned CometCore.sol; safe104/safe64 checks are reproduced; balance_of returns 0 for principal <= 0 and borrow_balance_of returns 0 for principal >= 0 as in pinned balanceOf/borrowBalanceOf.
- conformance::comet utilization uses the STORED indices and returns 0 when present total supply is 0, matching pinned getUtilization; accrued_indices returns stored indices when elapsed == 0 and errs when now < lastAccrualTime, matching the checked uint40 subtraction.
- Kinked rate comparator `utilization <= kink` and the branch structure match pinned getSupplyRate/getBorrowRate; the exact at-kink assertion in the conformance test is correct (824454591).
- Signed int104 decoding (two's complement, extremes -2^103 and 2^103-1) and unsigned bit extraction are correct and tested; tracking-only UserBasic writes produce no holder row.
- common/persist rules apply: reverted frames inside succeeded transactions drop, failed/reverted transactions contribute no storage writes (confirmed with a scratch run: Reverted tx with a userBasic write -> 0 rows, no error).
- reduce() correctly chains a valid same-block sequence (scratch run: 1->2 @10, 2->-3 @20 across two txs -> previous_value 1, value -3, change_count 2, first_ordinal 10, ordinal 20, provenance of the last tx) and refuses ties and discontinuities (tested).
- System-call writes are accepted with SCOPE_SYSTEM_CALL (scratch run: scope = 2); Block-level code changes on the Comet or the implementation fail closed (tested).
- Epoch cadence is correct for the fixture: BOUND at activation block 1, nothing at block 2, REAFFIRMED at 1001 with heartbeat 1000; 11 QUALIFIED_CONSTANT/DECLARATION rows and 2 Dependency rows (IMPLEMENTATION storage-pointer, UNDERLYING declared) are emitted with the binding.
- Preimage gathering includes frames whose storage changes touch the Comet even when Call.address differs (delegatecall through the transparent proxy), and preimages are verified with keccak before use.
- BlockClock carries chain_id, number, hash, parent, timestamp, state_root, producer_version, spec_revision, package identity, parameters_sha256 and consistent row counts (tested for global_state_count).
- `cargo test --locked -p compound-v3-balance-state` passes (6 tests) on the pinned toolchain; no repository files were modified (reproduction crate lives in the scratchpad).

## Finder `review:compound-v3:failclosed`

5 findings, 16 properties checked and found correct.

### Findings

- **[high]** `compound-v3/balance-state/tests/fixtures/mainnet-cusdcv3-epoch.json:19` (fail-closed-routine-block)  
  The committed cUSDCv3 configuration reviews no scalar `other_slots`, so the Comet reentrancy-guard slot written by every supply/withdraw/transfer/buyCollateral makes any routine block fail closed with `unresolved storage`.
  - Evidence: Fixture lines 19-25 list only `other_mapping_slots` [2,3,4,6,7]; there is no `other_slots` entry. lib.rs:543-551: `} else if market.other_slots.contains(&r.key) || market.other_mapping_slots.iter().any(|base| mapping_has_base(r.key, &preimages, base)) { /* reviewed */ } else { return Err(Error::msg(format!("unresolved storage for Comet 0x{} at key 0x{}; refusing incomplete balance state", ...))) }`. Pinned CometCore.sol: `bytes32 internal constant REENTRANCY_GUARD_FLAG_SLOT = bytes32(keccak256("comet.reentrancy.guard")); uint256 internal constant REENTRANCY_GUARD_NOT_ENTERED = 0; uint256 internal constant REENTRANCY_GUARD_ENTERED = 1;`. Pinned CometWithExtendedAssetList.sol: `function nonReentrantBefore() internal { bytes32 slot = REENTRANCY_GUARD_FLAG_SLOT; ... assembly ("memory-safe") { sstore(slot, REENTRANCY_GUARD_ENTERED) } }` and `function nonReentrantAfter() internal { ... assembly ("memory-safe") { sstore(slot, REENTRANCY_GUARD_NOT_ENTERED) } }`, applied on `function supplyInternal(...) internal nonReentrant`, `function transferInternal(...) internal nonReentrant`, `function withdrawInternal(...) internal nonReentrant`, `function buyCollateral(...) override external nonReentrant`. keccak256("comet.reentrancy.guard") = 0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac (computed locally and via the crate's own `keccak`). Reproduced with a scratchpad binary depending on the crate by path (/private/tmp/claude-501/-Users-denis-Github-substreams-evm-extended/df7da1af-462f-4031-9cb8-e8fa8422da54/scratchpad/comet-guard-check/src/main.rs): a SUCCEEDED tx with a proxy frame plus a DELEGATECALL frame writing key 0xc98c…53ac 0->1 (ordinal 10) then 1->0 (ordinal 20) on the Comet address yields `ERR: unresolved storage for Comet 0xc3d688b66703497daa19211eedff47f25384cdc3 at key 0xc98c7730ba19013824f711a9ab74801459b27e6ff7685cb924587c89aeda53ac; refusing incomplete balance state`. The pair is not dropped upstream: common/persist `storage_is_noop` (lib.rs:94-96) compares old/new per record and 0->1, 1->0 are each real changes; `reduce` (lib.rs:328-366) collapses them to old==new==0 but the key still reaches the unresolved branch.
  - Failure scenario: Any Ethereum mainnet block containing one successful cUSDCv3 `supply`, `withdraw`, `transfer`, `transferFrom`, `withdrawTo`, `supplyTo` or `buyCollateral` (i.e. essentially every block with Comet activity) produces two persisted Comet storage records on slot 0xc98c…53ac in the implementation's delegatecall frame. With the committed parameters the map returns Err for that block, so the module halts on the first block with user activity after activation; only absorb, pause, withdrawReserves and allow/allowBySig blocks would pass.
  - Suggested fix: Treat `keccak256("comet.reentrancy.guard")` as part of the qualified layout: either add it to `other_slots` in the fixture (and to the README's reviewed-slot list with its derivation), or, better, add a `reentrancy_guard_slot` field/constant to `Market` that parse checks equals keccak of the pinned name (as lido does for its named slots) and that project accepts. Add a regression test that feeds the 0->1 / 1->0 pair (ideally through a delegatecall frame) and asserts the block succeeds with no rows for that slot; optionally assert in `storage_noop`/reduce that the reduced guard word returns to 0.
  - Applies to siblings: compound-v2 reviews `_notEntered` (slot 0) and lido reviews the Aragon mutex by name; erc4626 models have no guard. Re-check each crate for any other slot written on every call.
- **[medium]** `compound-v3/balance-state/src/lib.rs:478` (invalidation)  
  An implementation-pointer write (a Comet governance upgrade) fails the block under every possible parameterization, so the upgrade block can only be skipped, silently dropping that block's holder and market writes; sibling packages emit an INVALIDATED epoch row with evidence instead.
  - Evidence: lib.rs:478 `require(r.key != market.implementation_slot, "Comet implementation pointer changed; requalify the epoch")?;` runs for every active market; lib.rs:450 `active` = markets with `activation_block <= block.number`; lib.rs:257 `require(markets.iter().all(|other: &Market| other.comet != market.comet), "duplicate market")?;` forbids listing a successor epoch for the same Comet. Same for code changes at lib.rs:456-459. Contrast compound-v2/balance-state/src/lib.rs:634-648 `fn invalidation(...) -> pb::ModelEpoch { reason, evidence_slot: r.key, evidence_previous_word: r.old, evidence_word: r.new, ..epoch_row(.., Invalidated) }` used at lib.rs:700-705 for `ImplementationPointerWrite`, and lido/balance-state/src/lib.rs:499-511 / 629-639 (INVALIDATED for contract-version and code change), erc4626 README line 40. proto/v1/balance_state.proto: `INVALIDATION_REASON_IMPLEMENTATION_POINTER_WRITE = 1; // Persisted write to the market's implementation pointer slot.` and `EPOCH_EVENT_KIND_INVALIDATED = 3; // The previous epoch ended at (block, ordinal) for reason, with the on-chain evidence cited ... Immediately followed by the BOUND row of the next epoch, or by SUSPENDED.` Comet upgrades are routine: every governance rate change deploys a new implementation (README lines 25-27, lib.rs:615-616).
  - Failure scenario: Block N contains the governance `upgrade(proxy, newImpl)` plus ordinary user supplies/withdraws. Under epoch-1 params the block fails at line 478. Re-parameterizing with epoch 2 (`activation_block: N`, new `implementation`) still fails at line 478 because the pointer write is in the same block. The only way to advance is `activation_block: N+1`, which skips the whole market for block N: every persisted `userBasic`, indices and totals write in block N (including the many user transactions batched with the proposal execution) is never emitted, and downstream consumers hold stale principals with no signal.
  - Suggested fix: Follow compound-v2/lido: on a pointer write (or code change) emit `ModelEpoch` INVALIDATED with `reason = IMPLEMENTATION_POINTER_WRITE` / `CODE_CHANGE` and evidence (`r.old`, `r.new`, ordinal, tx), keep decoding the block's other writes, and let parameters carry the successor epoch (relax `duplicate market` to duplicate `(comet, epoch)` with strictly increasing activation blocks, and emit the successor's BOUND row in the same block when its `implementation` equals the new pointer word). Add a test covering the upgrade block.
  - Applies to siblings: aave/balance-state also fails the block on a pointer write or code change; compound-v2, lido and erc4626 emit INVALIDATED rows.
- **[medium]** `compound-v3/balance-state/src/tests.rs:65` (test-coverage)  
  No test exercises the real deployment shape where Comet storage is written from a DELEGATECALL frame whose `Call.address` is the implementation (or CometExt), so the clause that makes preimage collection work for the proxied contract is unprotected.
  - Evidence: tests.rs:59-72 `user_basic_call` builds `eth::Call { index: 1, address: m.comet.clone(), keccak_preimages: ..., storage_changes: vec![write(...)] }` and tests.rs:122-129 / 207-212 / 214-224 all use `address: m.comet.clone()`. The production path depends on lib.rs:461-467 `if !active.iter().any(|m| call.address == m.comet || call.storage_changes.iter().any(|c| c.address == m.comet)) { continue; }` because cUSDCv3 is a TransparentUpgradeableProxy: the Comet implementation executes under DELEGATECALL (Firehose records the code address in `Call.address`, the proxy in `StorageChange.address`; cf. erc20/balances/tools/src/trace_context.rs:2 'DELEGATECALL and CALLCODE retain their caller's account'), and `allow`/`allowBySig` run in a nested delegatecall to CometExt whose address matches neither `comet` nor `implementation`.
  - Failure scenario: A refactor that simplifies line 464 to `call.address == m.comet` passes all six tests. On mainnet the `userBasic[account]` Keccak preimage lives only in the implementation's delegatecall frame, so it is never collected; the principal write then falls through to the `unresolved storage` branch (lib.rs:546-551) and every block with a supply/withdraw fails. Conversely a regression that dropped the `storage_changes` filter for writes would also go unnoticed.
  - Suggested fix: Add a test with a proxy frame (`address = comet`, no writes) and a child `eth::Call { call_type: CallType::Delegate, address: implementation, keccak_preimages, storage_changes on comet }` asserting a HolderBasis row is produced, plus a CometExt-shaped frame (address = a third address) writing `isAllowed` through the two-hop chain. The scratchpad reproduction in finding 1 already has this shape and can be adapted.
  - Applies to siblings: the same test gaps (failed/reverted transactions, same-block repeats, delegatecall frame shape, validate_block refusals, pre-activation blocks, multi-market blocks, determinism) exist in the three sibling map crates.
- **[medium]** `compound-v3/balance-state/src/lib.rs:229` (parameter-consistency)  
  `base_scale`, `base_index_scale` and `factor_scale` are accepted as arbitrary decimals and never cross-checked against `base_decimals` or the hard-coded `BASE_INDEX_SCALE`/`FACTOR_SCALE` the map stamps on every row, so inconsistent parameters emit self-contradictory metadata instead of being rejected.
  - Evidence: lib.rs:229-231 `(pb::StateField::CometBaseScale, decimal(&i.base_scale, "base_scale")?, "1"), (pb::StateField::CometBaseIndexScale, decimal(&i.base_index_scale, ...)?, "1"), (pb::StateField::CometFactorScale, decimal(&i.factor_scale, ...)?, "1")` with only `decimal()` (lib.rs:66-72: non-empty ASCII digits). lib.rs:35-36 `const BASE_INDEX_SCALE: &str = "1000000000000000"; const FACTOR_SCALE: &str = "1000000000000000000";` are used for `scale` of index rows (lib.rs:489), of rate immutables (lib.rs:197-228) and for `basis_scale` (lib.rs:571); `balance_decimals: market.base_decimals` (lib.rs:580) is independent of `base_scale`. Pinned CometCore.sol: `uint64 internal constant BASE_INDEX_SCALE = 1e15; uint64 internal constant FACTOR_SCALE = 1e18;` (not configurable); pinned constructor: `decimals = decimals_; baseScale = uint64(10 ** decimals_);`. README line 57 promises 'non-decimal immutables ... parameters rejected' but consistency is not checked.
  - Failure scenario: Parameters with `base_index_scale: "1000000000000000000"` (a plausible 1e18 typo) parse successfully; the block then carries a `COMET_BASE_INDEX_SCALE` QUALIFIED_CONSTANT row of 1e18 while `ModelEpoch.basis_scale` and every index row's `scale` say 1e15. Likewise `base_decimals: 6` with `base_scale: "1000000000000000000"` emits `balance_decimals = 6` next to `COMET_BASE_SCALE = 1e18`. A consumer that trusts the constant rows evaluates balances off by 10^3 or 10^12.
  - Suggested fix: In `parse`, require `base_index_scale == BASE_INDEX_SCALE`, `factor_scale == FACTOR_SCALE` and `base_scale == 10^base_decimals` (pinned constructor), or remove the three from `Immutables` and emit them from the constants; add a rejection test.
  - Applies to siblings: compound-v2 and erc4626 also echo caller-supplied constants without cross-checks.
- **[low]** `compound-v3/balance-state/src/lib.rs:335` (diagnostics)  
  The `ambiguous` / `discontinuous` / `no execution ordinal` refusals do not name the Comet address, slot or ordinals, unlike the `unresolved storage` message, so a halted stream cannot be diagnosed from the error alone.
  - Evidence: lib.rs:332 `require(w.ordinal > 0, "persisted storage write has no execution ordinal")?;` lib.rs:335 `require(w.ordinal > r.ordinal, "ambiguous storage execution order")?;` lib.rs:336 `require(w.old == r.new, "discontinuous storage writes within block")?;` versus lib.rs:547-551 which formats `0x{address}` and `0x{key}`. compound-v2 at least labels the kind (`format!("ambiguous {what} execution order")`, lib.rs:496).
  - Failure scenario: A producer bug or layout surprise yields a continuity break on one of hundreds of `userBasic` keys in a block; the operator sees only `discontinuous storage writes within block` with no key, ordinals or transaction to inspect and must rebuild the block offline to locate it.
  - Suggested fix: Include `hex::encode(&w.address)`, `hex::encode(w.key)`, the two ordinals and (for discontinuity) the previous new word / current old word in the messages.
  - Applies to siblings: lido and erc4626 reuse the same terse reduce() messages; compound-v2 labels the kind only.

### Checked and found correct

- evm-persist is the only ingestion path (lib.rs:454 persist::collect_block): storage writes reach the map only from SUCCEEDED-tx frames with state_reverted == false and from non-reverted Block.system_calls; FAILED/REVERTED transactions contribute no storage (persist lib.rs:156-203 emits gas balance, sender nonce and 7702 authority nonce/code only, all ignored by Collected); block-level balance/code records handled; per-record no-op writes dropped upstream (persist lib.rs:94-96, 111-118).
- reduce() (lib.rs:328-366): stable global sort by ordinal, per-(address,key) strictly increasing ordinal (tie -> 'ambiguous'), continuity old == previous new ('discontinuous'), ordinal 0 rejected, first_ordinal/ordinal/count/tx provenance tracked, BTreeMap iteration deterministic; tested at tests.rs:244-253.
- Keccak preimages (lib.rs:468-473): hex-decoded, recomputed with tiny-keccak and compared to the 32-byte key; holder attribution requires a 64-byte preimage with 12 zero bytes and base == user_basic_slot (lib.rs:506-509); non-address keys under slot 5 fall through to the unresolved error; preimages from reverted frames are used only as verified hash facts.
- Storage context: writes are filtered by StorageChange.address == comet (lib.rs:475), never by Call.address; preimage frames are selected by Call.address == comet OR any storage change on comet (lib.rs:461-467), identical to the qualified erc20/balances (lib.rs:227), aave, lido and compound-v2 patterns, so delegatecall (implementation, CometExt) frames are handled in code.
- Slot inference matches the pinned CometStorage.sol declaration order: slot 0 = baseSupplyIndex|baseBorrowIndex|trackingSupplyIndex|trackingBorrowIndex (4x uint64, offsets 0/64/128/192); slot 1 = totalSupplyBase(uint104 @0)|totalBorrowBase(uint104 @104)|lastAccrualTime(uint40 @208)|pauseFlags(uint8 @248); mappings totalsCollateral=2, isAllowed=3, userNonce=4, userBasic=5, userCollateral=6, liquidatorPoints=7. CometMainInterface, CometCore, CometConfiguration, CometMath and CometExt declare no additional state variables (only constants/immutables/structs), so no slot shift.
- UserBasic.principal is int104 at bits 0..104 (struct `int104 principal; uint64 baseTrackingIndex; uint64 baseTrackingAccrued; uint16 assetsIn; uint8 _reserved` = 32 bytes); signed_bits two's complement decoding verified at int104 extremes (tests.rs:93-101) and sign bit index computation (lib.rs:88) is correct.
- All reviewed mapping value structs fit one 32-byte word (TotalsCollateral 128+128, UserCollateral 128+128, LiquidatorPoints 32+64+128+32, UserBasic 104+64+64+16+8), so single-key resolution without key+N struct words is sufficient; two-level mappings isAllowed[owner][manager] and userCollateral[account][asset] resolve through mapping_has_base's preimage chain with a cycle guard (lib.rs:374-387, tested tests.rs:198-213).
- Routine Comet operations at the pinned source write only: slot 0 (accrueInternal), slot 1 (accrueInternal/supplyBase/withdrawBase/transferBase/absorbInternal/pause), userBasic[account] (updateBasePrincipal, updateAssetsIn, absorbInternal), userCollateral, totalsCollateral, isAllowed, userNonce, liquidatorPoints, and the reentrancy guard slot (finding 1); all but the guard are covered by the fixture.
- Fail-closed on layout surprises: unknown Comet key -> Err naming address and key; implementation-pointer write -> Err (checked before slot decoding, lib.rs:478); persisted code change on comet or implementation at any scope -> Err (lib.rs:456-459); malformed storage records (>32 bytes) counted and fail the block (lib.rs:293-306, 455); non-Comet writes ignored by design.
- Holder rows are emitted only when the decoded principal changed (tracking/assetsIn-only writes skipped, lib.rs:515-517); totals-word rows emitted only for changed fields (lib.rs:502-504); previous/raw words come from the first write's old and last write's new.
- Integer parsing: bits() extracts [offset, offset+width) from the big-endian word into a fresh word and uses BigInt::from_unsigned_bytes_be; decimal to_string output; no floats; offsets/widths constant and within 256.
- Output ordering is deterministic and total: holder_basis (market, holder, ordinal), global_state (market, field, key, ordinal), epochs (market, epoch), dependencies (market, epoch, role, contract); one reduced row per (address, key) so no equal sort keys; exactly one BlockClock per block with counts.
- Model constants match the pinned source: balanceOf = principal > 0 ? principal * accruedSupplyIndex / BASE_INDEX_SCALE : 0 (floor), BASE_INDEX_SCALE 1e15, FACTOR_SCALE 1e18, per-second rates = perYear / SECONDS_PER_YEAR (constructor), kinked getSupplyRate/getBorrowRate; conformance::comet constructor-scaling test reproduces the fixture immutables (1_030_568_239, 12_683_916_793, 0, 1_109_842_719, 7_927_447_995, 475_646_879).
- validate_block: Extended detail level, block.ver in producer_versions, 32-byte hash/parent/state root, header number == block.number > 0, timestamp present and non-negative, every tx status in 1..=3 with non-empty calls.
- Parameters fail closed: deny_unknown_fields on all structs, chain_id > 0, non-empty positive producer versions, positive epoch/activation, non-empty model_id/source_pin, 20-byte addresses and 32-byte slots, decimal-only immutables, overlap checks among indices/totals/implementation/user_basic and other_slots/other_mapping_slots, duplicate market rejected; parameters_sha256 stamped on the clock.
- WASM handler gating: only `#[cfg(target_arch = "wasm32")] mod handler` with `#[substreams::handlers::map] fn map_events` (lib.rs:665-673); wasm32 dependency tree = evm-persist, hex, proto, serde, serde_json, sha2 (default-features = false), substreams, substreams-ethereum, tiny-keccak; prost/prost-types dev-only; no conformance, num-bigint, tokio or reqwest; `cargo test --locked`, `cargo clippy --locked --all-targets -- -D warnings` and `cargo check --locked --target wasm32-unknown-unknown` all pass on the pinned 1.88 toolchain.

## How to finish the review

1. Wait for the subagent spend limit to reset, then re-run the workflow script saved under the session directory
   (`workflows/scripts/harden-new-balance-state-packages-wf_c5514d01-2ea.js`, run id `wf_c5514d01-2ea`) with
   `resumeFromRunId`; the four completed finders replay from cache. If the session directory is gone, the finder and
   verifier prompts are reproduced in [`skills/balance-state-package/SKILL.md`](../skills/balance-state-package/SKILL.md)
   under "Review checklist".
2. Or review by hand, crate by crate, with the same four dimensions; record results in this file and fix in one PR per crate.

